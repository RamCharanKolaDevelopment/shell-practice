#!/bin/bash

# In shell scripting 1st line should be start with “Shebang” which is “#!/bin/bash” this text always should be in the 1st line in all your shell scripting files(.sh files). 
# The “shebang” is a shell scripting interpreter of shell scripting code which checks and compile shell script code if any errors throughs error otherwise run your code and show output/execute script.
# The # is used to write comment also. Except 1st line remaining all place hash(#) is used to write comments.

echo "Hello world!"
# “echo” is used as print, to print text like “Hello World” like print/println in python programming language.

echo "I am your second shell script message"